# Prayas Chess Academy — Static Website

A simple, responsive static website for Prayas Chess Academy (HTML, CSS, JS).

## 🔎 Project Overview

- **Purpose:** Showcase the academy, classes, gallery, and provide contact information.
- **Stack:** Plain HTML, CSS, and JavaScript (no build step required).

## 🚀 Features

- Responsive header and navigation with mobile toggle
- Hero section with background image and CTA
- Highlights / feature cards
- Gallery grid for student photos
- Contact/CTA and footer with contact phone

## 📁 Project Structure

```
/ (project root)
├─ index.html
├─ about.html
├─ contact.html
├─ css/
│  └─ style.css
├─ js/
│  └─ main.js
└─ images/
   └─ students/
```

## ▶️ Preview Locally

Option 1 — Open directly:
- Open `index.html` in your browser.

Option 2 — Using a simple HTTP server (recommended for consistent behavior):

- With Python (3.x):

```powershell
# from project root
python -m http.server 8000
# then open http://localhost:8000
```

- With Node (http-server):

```powershell
npx http-server -c-1 .
# then open http://localhost:8080 (or shown port)
```

## ✍️ How to Edit / Contribute

- HTML: update `index.html`, `about.html`, or `contact.html`.
- CSS: modify `css/style.css` for styles and layout.
- JS: modify `js/main.js` for interactive behavior.
- Images: add photos to `images/` (e.g., `images/students/`) and update `index.html` or other pages.

If you add new assets, commit them and push to the repo. For feature changes, open a pull request and include screenshots.

## 🚩 Deployment

- GitHub Pages: push to the `main` branch and enable Pages from the repository settings (root folder).
- Netlify / Vercel: connect the repository and deploy from the main branch.

## 📝 License

This project is suitable for an MIT license or another permissive license. If you want, I can add a `LICENSE` file (MIT) for you.

## 📞 Contact

Prayas Chess Academy — Kalinganagar, Bhubaneswar
Call / WhatsApp: +91 99380 11913

---

*Created for the Prayas Chess Academy website — maintainers can update this README with project-specific details, a live demo link, or contribution guidelines.*
